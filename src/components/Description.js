// component to hold the description of the product
import React from "react";
import product from "../product";

const Description = () => {
  return <p>{product.description}</p>;
};

export default Description;
